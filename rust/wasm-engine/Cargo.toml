[package]
name = "consilium-wasm-engine"
version.workspace = true
edition.workspace = true
description = "CONSILIUM wasm-engine — procesamiento de documentos en el cliente: binarización B/N, compresión y SHA-256 (wasm-bindgen)."
license = "UNLICENSED"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
wasm-bindgen = "0.2"
image = { version = "0.25", default-features = false, features = ["png", "jpeg"] }
sha2 = "0.10"

[dev-dependencies]