{
  "name": "@consilium/server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "CONSILIUM Server — Backend (Fase 2). Registro remoto de documentos, presigned URLs y Outbox Worker con BullMQ.",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "build": "tsc -p tsconfig.json",
    "start:dev": "node --watch dist/main.js"
  },
  "dependencies": {
    "@apollo/server": "^4.11.3",
    "@consilium/contracts": "*",
    "@consilium/core": "*",
    "@nestjs/apollo": "^12.2.1",
    "@nestjs/common": "^10.4.15",
    "@nestjs/config": "^3.3.0",
    "@nestjs/core": "^10.4.15",
    "@nestjs/graphql": "^12.2.1",
    "bullmq": "^5.34.0",
    "graphql": "^16.10.0",
    "ioredis": "^5.4.1",
    "reflect-metadata": "^0.2.2"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}