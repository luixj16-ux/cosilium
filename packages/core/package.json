{
  "name": "@consilium/core",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "CONSILIUM Core — Dominio + Aplicación + Sincronización (append-only offline). Código puro compartido por web/desktop/mobile.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "build": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "vitest": "^3.0.4"
  }
}