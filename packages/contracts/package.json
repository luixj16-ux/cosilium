{
  "name": "@consilium/contracts",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "CONSILIUM Contracts — Tipos GraphQL y DTOs compartidos entre Core, UI, apps y backend.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./graphql": {
      "types": "./src/graphql/generated-types.ts",
      "default": "./src/graphql/generated-types.ts"
    }
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "build": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}